    <div class="row PhotoCaption">
        <div class="col-md-12">
            <form action="{{ url("zgh_fe.photos.caption.post_edit", {"id": photo.id}) }}" class="captionInput" method="post">
                {{ form_widget(form.caption) }}
                {{ form_errors(form.caption) }}
                <a href="#" data-url="{{ url("zgh_fe.photos.caption.index", {"id": photo.id}) }}" class="save_caption">Save</a>
                <a href="#" data-url="{{ url("zgh_fe.photos.caption.index", {"id": photo.id}) }}" class="cancel_caption">Cancel</a>
                {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
