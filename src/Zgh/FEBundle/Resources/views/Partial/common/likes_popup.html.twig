<ul class="ProfileList">
    {% for like in likes %}
        <li>
            <a class="pull-left" href="">
                <div class="imgProfileList">
                    <img alt="{{ url("zgh_fe.user_profile.index",{"id": like.user.id}) }}" src="{{ asset(like.user.profilePhoto.getThumbWebPath) }}"/>
                </div>

                <span>{{ like.user.fullname }}</span>
            </a>
            {{ renderFollow(like.user, "pull-right") }}
        </li>
    {% endfor %}
</ul>
